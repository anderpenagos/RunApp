<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme para o app (apenas para a Activity, o Compose cuida do resto) -->
    <style name="Theme.RunApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
